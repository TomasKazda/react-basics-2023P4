const Desk = ({id, suma, stools}) => {

    return (
        <div>
            <p>Stůl číslo {id} vypil {suma}</p>
            <div>
                ...
            </div>
        </div>
    )
}

export default Desk;